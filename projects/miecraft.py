import time,mcpi.minecraft as minecraft #imports time and the minecraft modual
mc = minecraft.Minecraft.create() #makes connection to game
time.sleep(5)
def num():
    range(10)
    num = 10
    while num == 10
    mc.postToChat range
    mc.postToChat("hello")
